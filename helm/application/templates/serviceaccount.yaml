{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "cloud-platform-app.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "cloud-platform-app.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
